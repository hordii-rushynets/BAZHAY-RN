export interface Article {
    slug: string;
    title: string;
    photo: string;
    description: string;
}

export interface Brand {
    slug: string;
    name: string;
    nickname: string;
    photo: string;
    description: string;
}